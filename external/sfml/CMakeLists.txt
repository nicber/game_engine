if(NOT ${SFML_MODULE} STREQUAL "")
	set(CMAKE_MODULE_PATH "${SFML_MODULE}" ${CMAKE_MODULE_PATH})
endif()

find_package(SFML 2 REQUIRED system window graphics network audio)

set(GAME_ENGINE_INCLUDE ${GAME_ENGINE_INCLUDE} ${SFML_INCLUDE_DIRS} PARENT_SCOPE)
set(GAME_ENGINE_DEP_LIBS ${GAME_ENGINE_DEP_LIBS} ${SFML_LIBRARIES} PARENT_SCOPE)