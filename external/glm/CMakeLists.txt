if(NOT ${GLM_MODULE} STREQUAL "")
	set(CMAKE_MODULE_PATH "${GLM_MODULE}" ${CMAKE_MODULE_PATH})
endif()

find_package(GLM REQUIRED)

set(GAME_ENGINE_INCLUDE ${GAME_ENGINE_INCLUDE} ${GLM_INCLUDE_DIR} PARENT_SCOPE)
