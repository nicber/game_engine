if(WIN32)
  set(AIO_IMPL win32.cpp)
else()
  set(AIO_IMPL unix.cpp)
endif()

list(APPEND GAME_ENGINE_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/${AIO_IMPL})
set(GAME_ENGINE_SRCS ${GAME_ENGINE_SRCS} PARENT_SCOPE)
