if(WIN32)
	set(TIME_IMPL win32.cpp)
else()
	set(TIME_IMPL unix.cpp)
endif()

list(APPEND GAME_ENGINE_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/${TIME_IMPL})
set(GAME_ENGINE_SRCS ${GAME_ENGINE_SRCS} PARENT_SCOPE)